<nav class="top-bar">
  <!-- Title -->
  <ul class="title-area">
    <li class="name"><h1><a href=<%= posts_path %>>TeachYoSelf</a></h1></li>

    <!-- Mobile Menu Toggle -->
    <li class="toggle-topbar menu-icon"><a href="#">Menu</a></li>
  </ul>

  <!-- Top Bar Section -->

<section class="top-bar-section">

    <!-- Top Bar Left Nav Elements -->
    <ul class="left">

      <!-- Search -->
          <li class="has-form">
          <div class="row collapse">
            <%= form_tag(posts_path, method: :get) do %>
              <div class="large-8 columns">
              <%= text_field_tag 'search', params[:query], placeholder: 'Search text here', size: 50 %>
              </div>
              <div class="large-4 small-3 columns">
              <%= submit_tag 'Search', class: "alert button expand" %>
            </div>
        <% end %>
        </div>
        </li>
      <li>
        <% if user_signed_in? %>
          <%= link_to "Profile", user_path(current_user), class: 'button' %>
        <% end %>
      </li>

      <li>
          <%= link_to "Share New Tutorial", new_post_path, class: 'button' %>
      </li>
      <!-- </div> -->
    </ul>

    <!-- Top Bar Right Nav Elements -->
    <ul class="right">
      <!-- Divider -->
      <li class="divider"></li>

      <!-- Button -->
      <% if user_signed_in? %>
        <li class="show-for-large-up">
        <%= link_to "Sign Out", destroy_user_session_path, method: :delete %>
      </li>
      <% else %>
      <li class="show-for-large-up">
        <%= link_to "Log in", new_user_session_path %>
      </li>
      <% end %>
    </ul>
  </section></nav>
